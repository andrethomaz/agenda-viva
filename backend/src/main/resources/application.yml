spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/agenda_viva}

server:
  port: ${PORT:8080}

agenda-viva:
  scheduler:
    ofertas-expiradas-ms: ${OFERTAS_EXPIRADAS_MS:60000}
